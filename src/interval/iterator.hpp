#ifndef PAPPUS_INTERVAL_ITERATOR_HPP
#define PAPPUS_INTERVAL_ITERATOR_HPP

// iterator<T> and subdivision<T> are defined in interval.hpp
// (they need interval<T> to be complete before their method bodies are compiled)
#include "interval.hpp"

#endif
